# Tuples 
# Tuples are like list but cannot be changed (immutable)
coordinates = (10, 20) # Creating a tuple
print( "X =", coordinates[0]) # Accessing first element
print( "Y =", coordinates[1]) # Accessing second element
